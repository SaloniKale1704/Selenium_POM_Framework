package com.pages;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import org.openqa.selenium.support.ui.Select;

public class HomePage {

	private WebDriver driver;

	public HomePage(WebDriver driver) {
		this.driver = driver;
		PageFactory.initElements(driver, this);

	}

	@FindBy(className = "product_sort_container")
	private WebElement filterOption;

	@FindBy(xpath = "//div[@id='inventory_container']//div[@class='inventory_item']")
	private List<WebElement> itemList;

	@FindBy(className = "inventory_details_price")
	private WebElement price;

	@FindBy(xpath = "//button[text()='ADD TO CART']")
	private WebElement addToCartBtn;

	// Actions 
	//Select suitable value from dropdown sort = hilo
	public void selectHighToLowRange(String sort) {
		Select dropdown = new Select(filterOption);
		dropdown.selectByValue(sort);
	}

	//Search item from list and click on it
	public void searchItem(String itemToSearch) {
		
		//traverse list of item
		for (WebElement item : itemList) {
			WebElement actualItem = item.findElement(By.xpath(".//a/div"));
			String expectedItem = actualItem.getText();
			
			//search for text from itemToSearch
			if(expectedItem.contains(itemToSearch)) {
				actualItem.click();
				break;
				
			} 
			//itemToSearch not found in list
			else {
				System.out.println(itemToSearch+" not available");
			}
		}
	}
	
	//get the actual price of item
	public void getItemPrice() {
		System.out.println(price.getText());
	}
	
	//navigate to cart page
	public void goToCartPage() {
		addToCartBtn.click();
	}

	/*
	 * //Filter // WebElement filterOption =
	 * driver.findElement(By.className("product_sort_container"));
	 * 
	 * 
	 * // List<WebElement> itemList = driver.findElements(By.xpath(
	 * "//div[@id='inventory_container']//div[@class='inventory_item']"));
	 * for(WebElement item : itemList) { WebElement jacketItem =
	 * item.findElement(By.xpath(".//a/div")); String expectedItem =
	 * jacketItem.getText(); System.out.println(expectedItem);
	 * if(expectedItem.contains("Fleece Jacket")) { jacketItem.click(); break; }
	 * else { System.out.println("Fleece Jacket not available"); } } //Add to cart
	 * 
	 * // WebElement price =
	 * driver.findElement(By.className("inventory_details_price"));
	 * System.out.println(price.getText());
	 * 
	 * // WebElement addToCartBtn =
	 * driver.findElement(By.xpath("//button[text()='ADD TO CART']"));
	 * addToCartBtn.click();
	 */

}
